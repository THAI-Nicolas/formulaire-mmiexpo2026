[build]
  command = "npm run build"
  publish = "dist"

[functions]
  # Increase payload size limit for file uploads
  # Default is 6MB, max is 10MB for synchronous functions
  payload_size = 10485760

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/__astro"
  status = 200
